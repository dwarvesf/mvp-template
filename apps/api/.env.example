DATABASE_URL=postgresql://postgres:postgres@localhost:35434/mvp_template
JWT_SECRET=mvp-template-dev-secret-key-change-in-production
PORT=4000

# Email Provider Configuration
# Provider options: resend, sendgrid, ses, mock
EMAIL_PROVIDER=mock
EMAIL_FROM=noreply@mvp-template.local
# EMAIL_FROM=onboarding@resend.dev # enable for testing with resend
EMAIL_REPLY_TO=support@mvp-template.local

# Provider-specific API Keys (only configure the one you're using)
RESEND_API_KEY=
RESEND_TEST_EMAIL=your-verified-email@domain.com  # For development with Resend test accounts
SENDGRID_API_KEY=
AWS_SES_REGION=us-east-1

# Frontend URL for email links
FRONTEND_URL=http://localhost:3000
